#设置前缀和后缀
thymeleaf:
  cache: false  #缓存
  mode: HTML
  suffix: .html
  prefix: classpath:/templates/

#数据源配置spring:
spring:
  datasource:
    password:
    username: root
    url: jdbc:mysql://localhost:3306/grope6design?useUnicode=true＆characterEncoding=utf-8＆useSSL=false&serverTimezone=GMT
    driver-class-name: com.mysql.cj.jdbc.Driver
#    type: com.alibaba.druid.pool.DruidDataSource